create database canteen

zô dtb canteen, excute all da command below 
--------------------------------------------

create table productType(
	productTypeID int AUTO_INCREMENT,
    productType varchar(255),
    PRIMARY KEY (productTypeID)
);

create table productPrice(
	productID int AUTO_INCREMENT,
    price float,
    PRIMARY KEY (productID)
);

create table employee(
	employeeID int AUTO_INCREMENT,
    employeeName varchar(255),
    employeeSex bit,
    employeeDOB Date,
    employeePhoneNumber varchar(255),
    employeeAddress varchar(500),
    PRIMARY KEY (employeeID)
);

create table productMenu(
    productID int AUTO_INCREMENT,
    productName varchar(255),
    productTypeID int,
    PRIMARY KEY (productID),
    FOREIGN KEY (productTypeID) REFERENCES productType(productTypeID)
);

create table bill(
	billID int AUTO_INCREMENT,
    purchaseDate DateTime,
    totalPrice float,
    employeeID int,
    PRIMARY KEY (billID),
    FOREIGN KEY (employeeID) REFERENCES employee(employeeID)
);

create table billDetail(
	billID int AUTO_INCREMENT,
    productID int,
    quantity int,
    PRIMARY KEY (billID),
    FOREIGN KEY (productID) REFERENCES productPrice(productID)
);

